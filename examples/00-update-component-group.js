/* eslint-disable no-undef */
const type = "update-component-group";

const id = 194280; // Replace with the actual ID of the component group you want to update

const group = {
  name: "Content Blocks", // New name for the group
};

module.exports = { type, id, group };
