/* eslint-disable no-undef */
const type = "update-datasource";

const id = "margin"; // Can use slug or ID

const datasource = {
  name: "Spacing: Margin", // Update the name for better organization
};

module.exports = { type, id, datasource };
