/* eslint-disable no-undef */
const type = "delete-story";

// You would need to get the actual ID from the Storyblok API or UI
const id = 456; // Replace with the ID of the story to delete

module.exports = { type, id };
